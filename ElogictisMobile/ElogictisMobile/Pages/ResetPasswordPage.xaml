<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ElogictisMobile.Pages.ResetPasswordPage">
    <ContentPage.Content>
        <StackLayout>
            <Entry Placeholder="Mật khẩu cũ" Text="{Binding OldPassword}"/>
            <Entry Placeholder="Mật khẩu mới" Text="{Binding NewPassword}"/>
            <Entry Placeholder="Xác nhận mật khẩu mới" Text="{Binding VerifyPassword}"/>
            <Button Text="Cập nhật"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>