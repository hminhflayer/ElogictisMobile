<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:buttons="clr-namespace:Syncfusion.XForms.Buttons;assembly=Syncfusion.Buttons.XForms"
             x:Class="ElogictisMobile.Views.FailedProductPage"
             NavigationPage.HasBackButton="True"
             NavigationPage.HasNavigationBar="True"
             Title="GIAO KHÔNG THÀNH CÔNG">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout>
                <Label Text="Lý do:"
                       FontSize="18"
                       FontAttributes="Bold"/>
                <Editor HeightRequest="300"
                        Text="{Binding TextFailed}"
                        Keyboard="Chat"/>
            </StackLayout>
            <StackLayout>
                <buttons:SfButton
                    Margin="0,16,0,0"
                    Style="{StaticResource DangerButtonStyle}"
                    Text="XÁC NHẬN"
                        HorizontalOptions="FillAndExpand"
                    Command="{Binding FailedProductCommand}"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>