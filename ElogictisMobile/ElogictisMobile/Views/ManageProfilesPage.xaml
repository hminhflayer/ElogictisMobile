<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="ElogictisMobile.Views.ManageProfilesPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:behavior="clr-namespace:ElogictisMobile.Behaviors;assembly=ElogictisMobile"
    xmlns:behaviors="clr-namespace:ElogictisMobile.Behaviors"
    xmlns:border="clr-namespace:Syncfusion.XForms.Border;assembly=Syncfusion.Core.XForms"
    xmlns:buttons="clr-namespace:Syncfusion.XForms.Buttons;assembly=Syncfusion.Buttons.XForms"
    xmlns:controls="clr-namespace:ElogictisMobile.Controls"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:data="clr-namespace:Syncfusion.DataSource;assembly=Syncfusion.DataSource.Portable"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sfavatar="clr-namespace:Syncfusion.XForms.AvatarView;assembly=Syncfusion.Core.XForms"
    xmlns:CI="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin"
    BackgroundColor="{DynamicResource Gray-Bg}"
    NavigationPage.HasNavigationBar="True"
    NavigationPage.HasBackButton="True"
    Title="Quản lý thành viên"
    mc:Ignorable="d">

    <ContentPage.Content>

        <ScrollView>
            <Grid
            ColumnSpacing="0"
            RowDefinitions="Auto, Auto, *"
            RowSpacing="0">

                <buttons:SfButton
                    Margin="0,16,0,16"
                    Style="{StaticResource GradientButtonStyle}"
                    Text="Thêm"
                    WidthRequest="200" 
                    Command="{Binding AddProfileCommand}"/>
                <ListView Grid.Row="1" ItemsSource="{Binding ProfilesList}" SelectionMode="None"
                          HasUnevenRows="True" ItemTapped="ListView_ItemTapped">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <StackLayout Orientation="Vertical"
                                             Spacing="16" Padding="16">
                                    <StackLayout Orientation="Horizontal">
                                        <Label Text="{Binding Auth_ext}"
                                                   FontAttributes="Bold"
                                                   TextColor="Green"
                                               FontSize="16"/>
                                    </StackLayout>
                                    <StackLayout Orientation="Horizontal">
                                        <CI:CircleImage Source="DefaultImage" 
                                                HeightRequest="45" 
                                                WidthRequest="45"
                                                BackgroundColor="AliceBlue" />
                                        <StackLayout Orientation="Horizontal">
                                            <StackLayout Orientation="Vertical">
                                                <Label Text="{Binding Name}"
                                           FontAttributes="Bold"
                                               FontSize="18"/>
                                                <Label Text="{Binding Email}"
                                               FontSize="16"/>
                                            </StackLayout>
                                            <StackLayout IsVisible="{Binding IsDelete}">
                                                <Label Text="ĐÃ XÓA" 
                                                   TextColor="Red"/>
                                            </StackLayout>
                                        </StackLayout>
                                    </StackLayout>
                                </StackLayout>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>