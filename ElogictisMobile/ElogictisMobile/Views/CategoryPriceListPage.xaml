<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="ElogictisMobile.Views.CategoryPriceListPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:buttons="clr-namespace:Syncfusion.XForms.Buttons;assembly=Syncfusion.Buttons.XForms"
    xmlns:converter="clr-namespace:ElogictisMobile.Converters"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:core="clr-namespace:Syncfusion.XForms.Core;assembly=Syncfusion.Core.XForms"
    xmlns:local="clr-namespace:ElogictisMobile.Views"
    xmlns:viewModel="clr-namespace:ElogictisMobile.ViewModels"
    BackgroundColor="{DynamicResource Gray-200}"
    Title="Danh mục bảng giá"
    NavigationPage.HasNavigationBar="True"
    NavigationPage.HasBackButton="True">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converter:BooleanToColorConverter x:Key="ColorConverter" />
            <converter:InverseBoolConverter x:Key="InverseBoolConverter" />
            <converter:NameValidationConverter x:Key="FirstValidationErrorConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>


    <ContentPage.Content>
        <ScrollView>
            <Grid
            ColumnSpacing="0"
            RowDefinitions="Auto, Auto, *"
            RowSpacing="0">

                <StackLayout Orientation="Horizontal">

                    <SearchBar x:Name="filterText" HeightRequest="40"
                               WidthRequest="300"
                            Placeholder="Nhập bảng giá" Grid.Row="1"
                            TextChanged="SearchBar_TextChanged"/>
                    <buttons:SfButton
                    Margin="0,16,0,20"
                    Style="{StaticResource GradientButtonStyle}"
                    WidthRequest="80" 
                    Text="Thêm"
                    Command="{Binding AddPriceListCommand}"/>
                </StackLayout>
                <ListView Grid.Row="1" ItemsSource="{Binding PricesList}" SelectionMode="None"
                          HasUnevenRows="True" ItemTapped="ListView_ItemTapped">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <StackLayout Orientation="Vertical" Spacing="16" Padding="0,20,0,0">
                                    <StackLayout Orientation="Vertical">
                                        <StackLayout BackgroundColor="Red">
                                            <Label Text="{Binding TypeShipProduct_ext}"
                                               FontSize="18"
                                               FontAttributes="Bold"
                                               TextColor="White"
                                               HorizontalOptions="CenterAndExpand"/>
                                        </StackLayout>
                                        <StackLayout Padding="16,0,0,0">
                                            <Label Text="{Binding TypeProduct_ext}"
                                               FontSize="18"
                                               FontAttributes="Bold"
                                               TextColor="Black"
                                               HorizontalOptions="StartAndExpand"/>
                                        </StackLayout>
                                        <StackLayout Orientation="Horizontal" Padding="16,0,0,0">
                                            <Label Text="{Binding From_Kilometer, StringFormat='Khoảng cách từ: {0} Km'}"
                                               FontSize="18"/>
                                            <Label Text="{Binding To_Kilometer, StringFormat='Đến: {0} Km'}"
                                               FontSize="18"/>
                                        </StackLayout>
                                        <StackLayout Orientation="Horizontal" Padding="16,0,0,0">
                                            <Label Text="{Binding From_Weight, StringFormat='Trọng lượng từ: {0} Kg'}"
                                               FontSize="18"/>
                                            <Label Text="{Binding To_Weight, StringFormat='Đến: {0} kg'}"
                                               FontSize="18"/>
                                        </StackLayout>
                                        <Label Text="{Binding Price, StringFormat='GIÁ TIỀN: {0:C} VND'}"
                                               FontSize="18"
                                               Padding="0,10,0,0"
                                               FontAttributes="Bold"/>
                                    </StackLayout>
                                    <BoxView 
                                       WidthRequest="1"
                                        HeightRequest="2"
                                       Color="Black"/>
                                </StackLayout>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </ScrollView>
    </ContentPage.Content>

</ContentPage>